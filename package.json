{
  "name": "lcaetano-basic-open-front-stack",
  "private": true,
  "scripts": {
    "start:dev": "lerna run start:dev --parallel"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.3.0"
  }
}
